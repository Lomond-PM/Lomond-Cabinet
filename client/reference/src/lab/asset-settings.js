export const seedAssetSettings=()=>({schemaVersion:1,motion:{curveId:null,durationMs:400},appearance:{accent:null,toolFill:null}});
export function validateAssetSettings(data){const id=v=>typeof v==='string'&&/^[-_A-Za-z0-9]{1,100}$/.test(v);if(!data||data.schemaVersion!==1||!data.motion||!data.appearance||!(data.motion.curveId===null||id(data.motion.curveId))||!Number.isFinite(data.motion.durationMs)||data.motion.durationMs<80||data.motion.durationMs>3000)throw new Error('Invalid UI motion settings.');for(const ref of [data.appearance.accent,data.appearance.toolFill])if(ref!==null&&(!ref||!id(ref.paletteId)||!id(ref.slotId)))throw new Error('Invalid paint reference.');return data;}
